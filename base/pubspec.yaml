name: base
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  login:
    path: ../login

  list:
    path: ../list

  dashboard:
    path: ../dashboard

  calculator:
    path: ../calculator

  lists:
    path: ../lists

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
